# 104 — 개발 환경 설정 & 프로젝트 구조

## 기술 스택

- **백엔드**: FastAPI, Python 3.11+, Supabase (supabase-py 2.x)
- **프론트엔드**: Next.js 14 (App Router), TypeScript, Tailwind CSS
- **AI**: Google Gemini API (`GOOGLE_API_KEY`)
- **영상**: FFmpeg, edge-tts, Pillow, PyMuPDF

## 코딩 규칙

- Python: `python -X utf8` 플래그 사용 (Windows CP949 인코딩 문제 방지)
- Python 가상환경: `apps/api/.venv` 사용
- `.env` 파일은 절대 git에 커밋하지 말 것

---

## 모델 역할 분담

| 별칭 | 모델 | 용도 |
|------|------|------|
| haiku | claude-haiku-4-5 | 파일 탐색, 간단한 수정, 테스트 |
| sonnet | claude-sonnet-4-6 | 코드 리뷰, 기능 구현 |
| opus | claude-opus-4-6 | 아키텍처 설계, 큰 구조 결정 |

---

## 프로젝트 구조

```
C:\LinkDropV3\
├── apps/
│   ├── api/                    # FastAPI 백엔드 (포트 8100)
│   │   ├── core/               # config.py / database.py
│   │   ├── routers/            # pipeline.py / steps.py / wiki.py / youtube.py
│   │   ├── services/           # world / script / wiki / keyframe / tts / render / casting
│   │   ├── agent/              # orchestrator.py / state_machine.py / retry_policy.py
│   │   ├── prompts/            # LLM 프롬프트 MD
│   │   └── data/               # art_styles.json, characters 정적 데이터
│   └── web/                    # Next.js 14 (포트 3100)
│       └── src/
│           ├── app/            # page.tsx / create/ / series/[id]/
│           ├── lib/            # seriesStore.ts (유일한 저장소 진입점)
│           ├── types/          # series.ts (전체 타입 정의 — 복사 금지)
│           └── components/     # PipelineStatus / SourceUploadEditor / KeyframeSetupEditor
├── packages/mcp-servers/       # MCP 서버 집중 관리
├── supabase/migrations/        # V3 전용 마이그레이션 (v3_ prefix)
├── .claude/rules/              # 모든 설계 규칙 문서 (50~106번)
└── .env                        # Supabase + Gemini + R2 키
```
