# 106 — 설계 문서 맵 (50~67번)

코드 수정 전 관련 문서를 반드시 확인할 것. 모든 문서는 `.claude/rules/` 에 위치.

| 문서 | 제목 | 핵심 요약 |
|------|------|----------|
| **50번** | 자동화 설계도 | Phase A-D 전략 / 1화 집중 원칙 / autoApprove 플래그 (`v3_series.settings`) |
| **51번** | 대본 작성 | 대본 생성 파이프라인 상세 규칙 |
| **52번** | 세계관 | resolution_methods + narrative_povs 2개 선택 → Gemini 제약 주입 / world_options.json v2.0 |
| **53번** | 마스터코드(씬과 컷) | **HOOK 2회 등장 = 설계 의도** / ch##s##nc## 체계 / dialogue 컷 상한 / **⚠️ 씬1 nc01은 의도적 공백** (없는 것이 정상, 있으면 버그) / **4컷 그룹화**: HOOK(idx=0) 제외 후 regularIdx=selectedIdx-1 (LD-013) |
| **54번** | 캐릭터 | 27명 풀 / `{id}_masako.json` + `{id}_noir_oil.json` / `fal_params.lora_url=null` (Phase D) |
| **55번** | TTS | 1캐릭터=1Supertone 성우 / edge-tts 폴백 / 27명 배정 완료 / EXTRA_VOICE_MAP |
| **56번** | ~~NLM키프레임~~ | **폐기됨** — 참조 금지 |
| **57번** | API 키프레임 | `image_hint` 우선 사용 / `image_prompt` 사용 금지 (photorealistic 오염) |
| **58번** | 키프레임 UI | bg_url / char_url / lipsync_url 마이그레이션 완료 / 컷 타입별 LEFT 버튼 분기 / **§12 자막 설계**: 폰트=`"Noto Serif KR","Gmarket Sans"` / 16:9=30자·9:16=25자·1줄 제한 / 타이핑→청크 즉시표시 |
| **59번** | 화풍 | `masako` + `noir_oil` 2종 확정 / `real` 삭제 완료 / art_styles.json 등록 완료 |
| **60번** | 캐스팅 | guest_cast POST-SCRIPT 후처리 **미구현** (`generate_guest_cast()` 없음) |
| **61번** | 이미지 프롬프트 | prompt_composer.py / cut_type 자동 분기 / OTS 구도 / build_bg_prompt / build_char_prompt |
| **62번** | 패럴랙스 렌더링 | `production` + `animation_type` 분기 → v3_scenes에 **미반영** (다음 구현 최우선) |
| **63번** | LLM 호출 전략 | **4단계 우선순위** Python→Cerebras→Gemini Free→Gemini Paid / `# ⚠️ GEMINI-PAID:` 마킹 |
| **64번** | 캐릭터 애니메이션 | **인형극 구조** (PuppetDialogueScene) MVP 완료 / 배경=2D / 캐릭터=3D 누끼 / mouthWRatio≤0.10 필수 |
| **65번** | 폴리스타일 레퍼런스 | 폴리스타일 캐릭터 레퍼런스 (기본 화풍) |
| **66번** | MP4합성 UI | `/series/mp4combine` 페이지 / 3단계 파이프라인(normalize→concat→final) / render_service.py / LD-001 HOOK 순서 보존 |
| **67번** | 다국어 TTS·더빙 분리 트랙 | `{code}_clean.mp4` + `{code}_sfx.mp3` + `{code}.mp3` → `_mux_final()` / clean_url·sfx_url DB 컬럼 / 재더빙 시 영상·SFX 재생성 불필요 |
