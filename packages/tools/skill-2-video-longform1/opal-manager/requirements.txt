# opal-manager 의존성
# 설치: pip install -r requirements.txt

httpx>=0.27.0          # HTTP 클라이언트
google-genai>=1.0.0    # Gemini API (google-generativeai 신규 통합 SDK)

# ── 영상 생산 파이프라인 (#2~#4) ─────────────────────────────────────────────
Pillow>=10.0.0         # 키프레임 PNG 생성 (그라데이션 폴백)
PyMuPDF>=1.24.0        # PDF → PNG 변환 (NotebookLM 슬라이드 다운로드 후)
edge-tts>=6.1.0        # Microsoft Edge Neural TTS (Google Cloud TTS 폴백)
google-cloud-texttospeech>=2.17.0  # Google Cloud TTS (API 활성화 필요)
